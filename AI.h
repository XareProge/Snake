#pragma once
#include <vector>
#include "Game.h"

// ─── Результат работы ИИ ─────────────────────────────────────────
struct AIResult {
    Dir             direction;  // следующее направление хода
    std::vector<Pt> path;       // полный путь до цели (для визуализации)
};

// Вычисляет лучший ход через BFS (поиск в ширину):
//   1. Ищет кратчайший путь к еде
//   2. Если пути нет — режим выживания (идёт к хвосту)
//   3. Если совсем заперт — любая безопасная клетка
AIResult computeAI(const Game& g);
